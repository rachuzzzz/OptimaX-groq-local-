# OptimaX Environment Configuration
# Copy this file to .env and update with your values

# ===========================================
# REQUIRED: Groq API Configuration
# ===========================================
# Get your free API key at: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# ===========================================
# Database Configuration
# ===========================================

# Option 1: Use included PostgreSQL container (Docker Compose)
# Uncomment for Docker deployment with included database
POSTGRES_DB=optimax_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/optimax_db

# Option 2: Use external PostgreSQL database
# Uncomment and modify for your external database
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Examples:
# Local database (non-Docker):
# DATABASE_URL=postgresql://postgres:mypassword@localhost:5432/traffic_db

# Production database:
# DATABASE_URL=postgresql://admin:securepass@db.production.com:5432/app_production

# AWS RDS:
# DATABASE_URL=postgresql://dbuser:pass@mydb.us-east-1.rds.amazonaws.com:5432/production

# ===========================================
# Notes
# ===========================================
# - When using Docker Compose with included postgres service, use 'postgres' as hostname
# - When running locally (not Docker), use 'localhost' as hostname
# - Never commit this file with real credentials to git
# - Add .env to .gitignore (already configured)
