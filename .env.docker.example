# ============================================================================
# OptimaX Docker Environment Variables
# ============================================================================
# Copy this file to .env and fill in the values before running docker-compose
# ============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Groq API Key
# -----------------------------------------------------------------------------
# Get your API key from: https://console.groq.com/keys
# Format: gsk_...
GROQ_API_KEY=gsk_your_api_key_here

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# If using the bundled PostgreSQL container, use this default:
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/optimax_db

# If connecting to an external database, use:
# DATABASE_URL=postgresql://user:password@host:5432/database_name

# PostgreSQL container configuration (only needed if using bundled postgres)
POSTGRES_DB=optimax_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
